intrinsic class net.wargaming.managers.ToolTipManager
{
    static function get instance();
    function show(string, initProps);
    function showType(tooltipType, string, initProps);
    function showComplex(str, props);
    function hide();
}
